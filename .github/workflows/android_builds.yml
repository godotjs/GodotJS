name: 🤖 Android Builds
on:
  workflow_call:
    inputs:
      version:
        required: true
        type: string
      deps_package_tag:
        description: 'deps package tag'
        type: string
        required: true

# Global Settings
env:
  SCONSFLAGS: verbose=yes warnings=extra werror=no strict_checks=yes debug_symbols=no module_text_server_fb_enabled=yes

# We only build template release - most developers will use a desktop device for editor
jobs:
  build-android:
    runs-on: ubuntu-24.04
    name: ${{ matrix.name }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Template (target=template_release, arch=arm64, v8)
            cache-name: android-template-v8
            target: template_release
            tests: false
            sconsflags: arch=arm64 swappy=yes
            cache-limit: 1
            engine: v8

          - name: Template (target=template_release, arch=arm64, qjs_ng)
            cache-name: android-template-qjs-ng
            target: template_release
            tests: false
            sconsflags: arch=arm64 swappy=yes use_quickjs_ng=yes
            cache-limit: 1
            engine: qjs-ng

    steps:
      - name: Checkout Godot
        uses: actions/checkout@v4
        with:
          repository: godotengine/godot
          ref: ${{ inputs.version }}
          submodules: recursive

      - name: Checkout GodotJS
        uses: actions/checkout@v4
        with:
          path: ${{github.workspace}}/modules/GodotJS/
          lfs: true
          submodules: recursive

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Restore Godot build cache
        uses: ./.github/actions/godot-cache-restore
        with:
          cache-name: ${{ matrix.cache-name }}
        continue-on-error: true

      - name: Deps v8
        if: matrix.engine == 'v8'
        shell: bash
        run: |
          curl https://github.com/ialex32x/GodotJS-Dependencies/releases/download/${{ inputs.deps_package_tag }}/${{ inputs.deps_package_tag }}.zip
          unzip ${{ inputs.deps_package_tag }}.zip -d /tmp/${{ inputs.deps_package_tag }}
          mv ${{ inputs.deps_package_tag }}/v8 godot/modules/GodotJS/v8

      - name: Setup Python and SCons
        uses: ./.github/actions/godot-deps

      - name: Download pre-built Android Swappy Frame Pacing Library
        uses: dsaltares/fetch-gh-release-asset@1.1.2
        with:
          repo: godotengine/godot-swappy
          version: tags/from-source-2025-01-31
          file: godot-swappy.7z
          target: swappy/godot-swappy.7z

      - name: Extract pre-built Android Swappy Frame Pacing Library
        run: 7za x -y swappy/godot-swappy.7z -o${{github.workspace}}/thirdparty/swappy-frame-pacing

      - name: Compilation
        uses: ./.github/actions/godot-build
        with:
          sconsflags: ${{ env.SCONSFLAGS }} ${{ matrix.sconsflags }}
          platform: android
          target: ${{ matrix.target }}
          tests: ${{ matrix.tests }}
          scons-cache-limit: ${{ matrix.cache-limit }}

      - name: Save Godot build cache
        uses: ./.github/actions/godot-cache-save
        with:
          cache-name: ${{ matrix.cache-name }}
        continue-on-error: true

      - name: Generate Godot templates
        if: matrix.target == 'template_release'
        run: |
          cd platform/android/java
          ./gradlew generateGodotTemplates
          cd ../../..
          ls -l bin/

      - name: Upload artifact
        uses: ./.github/actions/upload-artifact
        with:
          name: ${{ matrix.cache-name }}

